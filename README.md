# DNS-Response-Test
The dnstest.sh script is a Bash utility designed to test the DNS response times of a predefined list of common internet domains. It allows users to specify a DNS server and perform multiple tests to analyze the speed and reliability of DNS queries.
